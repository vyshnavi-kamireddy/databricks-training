# 📊 Week 5 • Day 3 | PySpark Interview-Oriented Practice

> *Strengthening PySpark fundamentals by reviewing commonly asked interview questions covering DataFrame transformations, RDD operations, joins, partitioning, caching, ranking functions, and aggregation techniques.*

---

## 🎯 Objective

Revise core PySpark concepts frequently asked in technical interviews by understanding the differences between commonly used transformations, actions, joins, ranking functions, and optimization techniques.

---

## 📂 Files Included

| File | Description |
|------|-------------|
| `Interview-Oriented_Practice.txt` | Collection of interview-oriented PySpark questions and concise explanations covering transformations, joins, partitioning, caching, ranking functions, and aggregations. |

---

## 🚀 What I Covered

### 🔹 RDD Transformations
- Compared `map()` and `flatMap()`
- Understood one-to-one vs one-to-many transformations

### 🔹 Aggregation Techniques
- Compared `groupBy()` and `reduceByKey()`
- Learned how local aggregation improves performance

### 🔹 Partition Optimization
- Differentiated between `repartition()` and `coalesce()`
- Understood shuffle behavior and performance implications

### 🔹 Storage Optimization
- Compared `cache()` and `persist()`
- Learned different storage strategies for repeated computations

### 🔹 DataFrame Operations
- Compared `union()` and `unionByName()`
- Differentiated `distinct()` and `dropDuplicates()`
- Understood `inner join` and `left join`

### 🔹 Analytical Functions
- Compared `rank()` and `dense_rank()`
- Differentiated `explode()` and `explode_outer()`
- Learned the use cases of `rollup()` and `cube()`

---

## 🛠️ Topics Covered

`map()` • `flatMap()` • `groupBy()` • `reduceByKey()` • `repartition()` • `coalesce()` • `cache()` • `persist()` • `union()` • `unionByName()` • `inner join` • `left join` • `distinct()` • `dropDuplicates()` • `rank()` • `dense_rank()` • `explode()` • `explode_outer()` • `rollup()` • `cube()`

---

## 🎯 Outcome

✔ Strengthened understanding of frequently asked PySpark interview concepts.

✔ Learned performance differences between commonly used transformations.

✔ Improved knowledge of DataFrame and RDD operations.

✔ Prepared concise explanations for technical interviews.

---

⭐ **Progress:** Successfully completed an interview-oriented PySpark revision covering DataFrame transformations, RDD concepts, joins, partitioning, ranking functions, and optimization techniques commonly asked in Data Engineering interviews.
