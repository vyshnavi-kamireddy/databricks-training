# 📊 Week 2 • Day 3 | SQL String, Numeric & Date Functions

> *Exploring SQL built-in functions to perform string manipulation, numerical calculations, date operations, and conditional business logic through practical assignment-based scenarios.*

---

## 🎯 Objective

Strengthen SQL fundamentals by practicing commonly used String, Numeric, and Date functions while solving real-world business scenarios involving employee records, customer transactions, subscriptions, loans, and financial reporting.

---

## 📂 Files Included

| File | Description |
|------|-------------|
| `Assignment_StrDataNum.docx` | Assignment document containing practice questions and business scenarios. |
| `Str_Data_Num_Ass_Basics.sql` | SQL solutions implementing String, Numeric, Date, and Conditional functions for multiple real-world datasets. |

---

## 🚀 What I Implemented

### 🔹 String Functions
- Converted text using `UPPER()`, `LOWER()`, and Proper Case formatting
- Extracted email domains and formatted text values
- Used `CONCAT()`, `LEFT()`, `SUBSTRING()`, and `SUBSTRING_INDEX()` for string manipulation

### 🔹 Numeric Functions
- Rounded values using `ROUND()`
- Truncated decimal values using `TRUNCATE()`
- Calculated absolute values using `ABS()`
- Formatted numeric values using `FORMAT()`
- Performed financial calculations for salaries, bonuses, discounts, and loan interest

### 🔹 Date & Time Functions
- Extracted years and month names
- Calculated experience and subscription durations using `TIMESTAMPDIFF()`
- Measured delivery delays and remaining days using `DATEDIFF()`
- Worked with `CURDATE()` for dynamic date-based analysis

### 🔹 Business Logic with CASE
- Classified employee experience levels
- Categorized customer spending patterns
- Evaluated delivery status and subscription validity
- Assessed loan risk levels
- Generated business-friendly status reports using conditional logic

---

## 📊 Practice Summary

- **10 assignment-based SQL problems completed**
- Worked with **10 different business datasets**
- Applied **20+ built-in SQL functions**
- Solved real-world scenarios involving HR, finance, banking, subscriptions, retail, and customer analytics

---

## 🛠️ Topics Covered

`UPPER()` • `LOWER()` • `CONCAT()` • `LEFT()` • `SUBSTRING()` • `SUBSTRING_INDEX()` • `ROUND()` • `TRUNCATE()` • `ABS()` • `FORMAT()` • `YEAR()` • `MONTHNAME()` • `DATEDIFF()` • `TIMESTAMPDIFF()` • `CURDATE()` • `CASE`

---

## 🎯 Outcome

✔ Strengthened practical knowledge of SQL String, Numeric, and Date functions.

✔ Learned to combine multiple built-in functions for real-world business reporting.

✔ Improved SQL problem-solving skills by implementing conditional logic across diverse datasets.

---

⭐ **Progress:** Successfully completed Day 3 by applying SQL built-in functions to solve business-oriented analytical and reporting scenarios.
